class _TodoDataContainer {
  String title;
  String description;
  int itemPos;
  bool isCompleted;
  String? author;

  _TodoDataContainer(this.title, this.description, this.itemPos, this.isCompleted, this.author);

}